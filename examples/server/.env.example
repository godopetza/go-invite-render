# go-invite-render HTTP server example
# Copy to .env and adjust as needed.
# The library itself needs no environment variables —
# fonts are bundled and rendering is fully local.

PORT=8080

# Optional: set to restrict to specific origins if you put this behind a gateway
# ALLOWED_ORIGIN=https://yourapp.com
